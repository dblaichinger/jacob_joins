- if @user && @recipe
  %h2 Hallo #{@user.name}, du kannst hier deine bestehenden CSIs ändern!
  %h2 Klicke auf Reset um ein neue CSIs hochzuladen!

#wizard
  #formnav
    = image_tag( 'bildzeichen.png')
    %ul
      %li
        = link_to 'Your Recipe', "#recipe_tab"
      %li
        = link_to 'About your country', "#country_specific_information_tab"
      %li
        = link_to 'About you', "#user_tab"
      %li
        = link_to 'preview', "#preview_tab"
    
  #recipe_tab
    = render :template => "recipes/new", :locals => { :recipe => Recipe.new, :steps => [Step.new] }
  #country_specific_information_tab
    = render :template => "country_specific_informations/new", :locals => { :csi_set => CsiSet.new(:country_specific_informations => CsiSet.empty_set) }
  #user_tab
    = render :template => "users/new", :locals => { :user => User.new }
  #preview_tab
    %h1 Nothing to do here...
